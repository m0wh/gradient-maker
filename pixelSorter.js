const imageToArray = src => {
  // Fetch image
  // Get pixels
  // Return the colors as an array
}

const arrayToImage = pixelsArray => {
  // Generates the image
}

module.exports = (src, pixelsArray) => {
  // Pixel sorting algorithm
  // Returns a pixels array
}